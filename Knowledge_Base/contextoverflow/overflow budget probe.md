# Overflow Budget Probe

Overflow budget probe validates deterministic no-provider RAG fallback under dense same-document evidence.

## Overflow Budget Probe Segment 01

Overflow budget probe segment 01 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 01 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 02

Overflow budget probe segment 02 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 02 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 03

Overflow budget probe segment 03 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 03 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 04

Overflow budget probe segment 04 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 04 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 05

Overflow budget probe segment 05 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 05 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 06

Overflow budget probe segment 06 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 06 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 07

Overflow budget probe segment 07 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 07 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 08

Overflow budget probe segment 08 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 08 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 09

Overflow budget probe segment 09 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 09 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 10

Overflow budget probe segment 10 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 10 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 11

Overflow budget probe segment 11 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 11 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 12

Overflow budget probe segment 12 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 12 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 13

Overflow budget probe segment 13 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 13 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 14

Overflow budget probe segment 14 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 14 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 15

Overflow budget probe segment 15 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 15 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 16

Overflow budget probe segment 16 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 16 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 17

Overflow budget probe segment 17 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 17 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.

## Overflow Budget Probe Segment 18

Overflow budget probe segment 18 records a distinct scoped evidence fragment for testing max-fragment pressure. Overflow budget probe answers must remain deterministic when no LLM provider is configured. The RAG context pack should keep direct support first, then include only as much parent context as the budget allows. Segment 18 is intentionally concise so the probe stresses fragment count rather than per-fragment truncation.
