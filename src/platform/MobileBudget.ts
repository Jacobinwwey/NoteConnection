/**
 * TypeScript projection of config/mobile-budget.v1.json for server-side export metadata.
 * Keep this small and compare it with the checked-in contract in mobile budget tests.
 */
export const MOBILE_BUDGET_SCHEMA_VERSION = 1;
export const MOBILE_LOW_ARTIFACT_BUDGET_BYTES = 25 * 1024 * 1024;
export const MOBILE_LOW_MAX_RESIDENT_BYTES = 256 * 1024 * 1024;
export const MOBILE_LOW_MAX_DEVICE_RAM_BYTES = 4 * 1024 * 1024 * 1024;
